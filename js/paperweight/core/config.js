/*
 * core/config.js
 * Paperweight config variables
 */
define({
    // Game setting values
    GAME_WIDTH: 1280,
    GAME_HEIGHT: 720,

    // Player physics values
    PLAYER_MAX_VELOCITY: 300,
    PLAYER_ACCELERATION: 300,
    PLAYER_DRAG: 300,
    PLAYER_REVERSE_DAMP_FACTOR: 0.8,
    PLAYER_BOUNCE: 0.5
});
