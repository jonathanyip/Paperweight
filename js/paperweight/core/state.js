/**
 * Global State Variables
 * Used to define things that are carried over between stages.
 *
 * @module core/state
 */
define(["core/game", "core/config"], function(game, Config) {
    "use strict";

    var Config = {
        // Nothing right here right now...
    };

    return Config;
});
